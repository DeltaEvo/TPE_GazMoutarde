<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Gaz Moutarde</title>
  <style>
    .step {
      width: 100vw;
      height: 100vh;
      text-align: center;
    }
  </style>
</head>
<body>
    {% macro class(name, value) -%}
        {% if value -%}
            {{ name }}="{{ value }}"
        {%- endif %}
    {%- endmacro %}

    <div id="impress">
        {% for slide in slides %}
        <div id={{ slide.name }}" class="step" {{ class('data-x' , slide.vars.x) }} {{ class('data-y' , slide.vars.y) }}
                                {{ class('data-z' , slide.vars.z) }} {{ class('data-rotate-x' , slide.vars.rotateX) }}
                                {{ class('data-rotate-y' , slide.vars.rotateY) }} {{ class('data-rotate-z' , slide.vars.rotateZ) }}
                                {{ class('data-scale' , slide.vars.scale) }}>
                {{ slide.data | safe }}
          </div>
        {% endfor %}
    </div>
    <script src="app.js"></script>
</body>
</html>
